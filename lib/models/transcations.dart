class Transcations {
  final String id;
  final String title;
  final double amount;
  final DateTime date;
  Transcations(
      {required this.id,
      required this.title,
      required this.amount,
      required this.date});
}
